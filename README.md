# KubeEye - Инструмент инспекции кластеров Kubernetes

Эта версия kubeeye расположена здесь - https://github.com/optical4eye/kubeeye/


## Обзор

KubeEye - это **чисто observational** инструмент инспекции кластеров Kubernetes, ориентированный на безопасный сбор информации о кластере и выявление потенциальных проблем. Инструмент предоставляет современный веб-интерфейс React с FastAPI backend, поддерживающий различные методы инспекции.

**Гарантия безопасности**: KubeEye использует строгую политику инспекции только для чтения, все операции ограничены сбором информации и просмотром статуса, никакие модификации, удаления или опасные операции никогда не выполняются, обеспечивая безопасность кластера.

## Основные возможности

- **Безопасность прежде всего**: Обязательный режим только для чтения, все команды инспекции проходят строгие проверки безопасности, обеспечивая нулевой риск
- **Управление информацией о кластерах**: Поддержка настройки и управления информацией о подключении для нескольких кластеров
- **Различные методы инспекции**: Статус узлов, метрики Prometheus, соответствие правилам OPA
- **Визуальные отчеты**: Интуитивное отображение результатов инспекции, включая графики и подробные описания проблем
- **Запрос истории**: Поддержка просмотра истории результатов инспекции и анализа тенденций
- **Рекомендации по исправлению**: Предоставление рекомендаций по решениям для выявленных проблем
- **Шифрование чувствительной информации**: Защита паролей подключения к кластеру и другой чувствительной информации с использованием алгоритмов шифрования
- **Управление логами**: Единая система логирования для удобного отслеживания и диагностики проблем
- **Мониторинг сертификатов**: Автоматическая проверка сроков действия сертификатов kubeconfig, предварительные предупреждения
- **Стандартизированное логирование**: Все компоненты используют единую функцию get_logger для структурированного логирования с префиксом "kubeeye."

## Функции безопасности

### Чисто observational дизайн
- **Принцип только для чтения**: Все операции инспекции ограничены извлечением информации и просмотром статуса
- **Режим whitelist**: Разрешено выполнение только явно безопасных команд, все неизвестные команды запрещены по умолчанию
- **Многоуровневые проверки безопасности**: Строгие проверки безопасности перед выполнением команд
- **Аудит лог**: Полная запись всех операций и событий безопасности

### Меры безопасности
- **Запрет операций модификации**: Опасные команды такие как `rm`, `chmod`, `systemctl restart` и т.д. запрещены
- **Запрет операций записи**: Запрещены действия записи, такие как перенаправление файлов, создание файлов и т.д.
- **Запрет операций установки**: Установка программного обеспечения такая как `apt install`, `pip install` и т.д. не разрешена
- **Обязательный безопасный режим**: Невозможно снизить уровни безопасности через настройки

## Архитектура

### Версия 3.3 - Сервис-ориентированная архитектура

- **Frontend**: React приложение, обслуживаемое Nginx
- **Backend**: FastAPI приложение с бизнес-логикой
- **База данных**: Файловое хранилище (JSON файлы)
- **Связь**: REST API между frontend и backend

### Компоненты

```
┌─────────────────┐    REST API    ┌─────────────────┐
│   React Frontend│◄──────────────►│  FastAPI Backend │
│     (Nginx)     │                │   (Python)      │
└─────────────────┘                └─────────────────┘
         │                                   │
         └────────────► Browser ◄────────────┘
```

### Структура Backend

Backend организован по принципам сервис-ориентированной архитектуры с четким разделением ответственности:

```
backend/
├── scripts/                    # Исполняемые скрипты и главный entry point
│   ├── api.py                  # FastAPI приложение (main entry)
│   ├── init.py                 # Скрипт инициализации
│   └── cleanup_reports.py      # Очистка отчетов
├── services/                   # Бизнес-сервисы (ядро функционала)
│   ├── inspectors/             # Логика инспекций Kubernetes
│   │   ├── base_inspector.py   # Базовый класс инспекторов
│   │   ├── controller.py       # Контроллер инспекций
│   │   ├── inspector_registry.py # Реестр инспекторов
│   │   ├── rule_processor.py   # Обработка правил
│   │   ├── node/               # Инспекторы узлов
│   │   ├── opa/                # OPA инспекторы
│   │   └── prometheus/         # Prometheus инспекторы
│   └── components/             # Дополнительные компоненты бизнес-логики
│       └── inspection_engine.py # Движок инспекций
├── controllers/                # API контроллеры (обработка HTTP запросов)
│   ├── main.py                 # Главный контроллер FastAPI
│   ├── clusters.py             # Управление кластерами
│   ├── inspection.py           # Инспекции
│   ├── reports.py              # Отчеты
│   ├── rules.py                # Правила
│   ├── scheduled_tasks.py      # Запланированные задачи
│   ├── gitops.py               # GitOps
│   ├── cleanup.py              # Очистка
│   └── network.py              # Сетевые проверки
├── infrastructure/             # Инфраструктурный код (утилиты, клиенты)
│   ├── cluster/                # Kubernetes/кластерные утилиты
│   │   ├── k8s_client.py       # K8s API клиент
│   │   ├── cluster_config.py   # Конфигурация кластеров
│   │   ├── node_connection.py  # SSH соединения
│   │   └── node_parser.py      # Парсинг узлов
│   ├── security/               # Безопасность и шифрование
│   │   ├── command_security.py # Проверки команд
│   │   ├── crypto_utils.py     # Криптография
│   │   └── ssh_connection_pool.py # Пул SSH соединений
│   ├── logging/                # Логирование
│   │   ├── logging_config.py   # Конфигурация логирования (get_logger функция)
│   │   └── enhanced_logging.py # Расширенное логирование
│   ├── network/                # Сетевые проверки
│   │   └── network_check.py    # Сетевые утилиты
│   ├── prometheus/             # Prometheus клиент
│   │   └── prometheus_client.py # Prometheus API
│   ├── gitops/                 # GitOps менеджер
│   │   └── gitops_manager.py   # Управление GitOps
│   ├── rules/                  # Управление правилами
│   │   ├── rule_loader.py      # Загрузка правил
│   │   ├── rule_manager.py     # Менеджер правил
│   │   └── validate_node_rules.py # Валидация правил
│   ├── results/                # Обработка результатов
│   │   ├── inspection_result.py # Результаты инспекций
│   │   ├── result_extractor.py # Извлечение результатов
│   │   └── result_formatter.py # Форматирование результатов
│   ├── tasks/                  # Управление задачами
│   │   ├── schedule_manager.py # Планировщик задач
│   │   ├── task_queue.py       # Очередь задач
│   │   └── task_execution.py   # Выполнение задач
│   ├── common/                 # Общие утилиты
│   │   ├── assertion_manager.py # Проверки
│   │   ├── common.py           # Общие функции
│   │   ├── data_cleanup.py     # Очистка данных
│   │   ├── exceptions.py       # Исключения
│   │   └── version.py          # Версии
│   └── config/                 # Конфигурационные файлы
│       ├── security.yaml       # Конфигурация безопасности
│       └── security_backup.yaml # Резервная копия
├── tests/                      # Тестовая инфраструктура
│   ├── conftest.py             # Фикстуры pytest
│   ├── integration/            # Интеграционные тесты
│   └── unit/                   # Unit тесты
├── Dockerfile                  # Docker конфигурация backend
├── requirements.txt            # Python зависимости
├── .dockerignore               # Исключаемые файлы для Docker
└── __init__.py
```

#### Описание основных директорий:

- **scripts/**: Исполняемые скрипты, главный entry point (`api.py`) и инициализация
- **services/**: Бизнес-логика приложения - инспекторы Kubernetes и компоненты
- **controllers/**: HTTP API контроллеры FastAPI с роутами и обработкой запросов
- **infrastructure/**: Низкоуровневые утилиты, клиенты API, безопасность, логирование
- **tests/**: Комплексная тестовая инфраструктура с unit и integration тестами

## Тестирование

KubeEye включает комплексную тестовую инфраструктуру для обеспечения качества и надежности кода. Все тесты запускаются в изолированной Docker среде с использованием pytest.

### Запуск тестов

```bash
# Запуск всех тестов
./run_test.sh

# Только unit тесты
./run_test.sh --unit

# Только integration тесты
./run_test.sh --integration

# С генерацией отчета о покрытии
./run_test.sh --coverage
```

### Структура тестов

Тесты организованы по уровням и областям ответственности:

```
backend/tests/
├── conftest.py                    # Фикстуры для всех тестов
├── integration/                   # Интеграционные тесты
│   └── test_api_endpoints.py     # Тесты API эндпоинтов
└── unit/                         # Unit тесты
    ├── test_infrastructure/      # Тесты инфраструктурных компонентов
    │   ├── test_network_check.py # Сетевые проверки
    │   └── test_cluster/         # Кластерные утилиты
    └── test_services/            # Тесты бизнес-логики
        └── test_inspectors/      # Тесты инспекторов
```

### Категории тестов

#### 🔧 **Integration Tests** (6 тестов)
**Тестируют взаимодействие компонентов и API:**

1. **`TestAPIEndpoints`** (5 тестов):
   - `test_health_check` - проверка основного эндпоинта `/`
   - `test_api_info` - информация о API (`/api/info`)
   - `test_clusters_list_empty` - список кластеров при пустой БД
   - `test_invalid_endpoint` - обработка 404 ошибок
   - `test_root_endpoint` - корневой эндпоинт с документацией

2. **`TestAPIErrorHandling`** (1 тест):
   - `test_method_not_allowed` - проверка 405 ошибок
   - `test_invalid_json` - валидация JSON запросов

#### 🧬 **Unit Tests** (7 тестов)
**Тестируют отдельные компоненты изолированно:**

1. **`TestNetworkValidation`** (4 теста):
   - `test_validate_ip_valid/invalid` - валидация IP адресов
   - `test_validate_port_valid/invalid` - валидация портов

2. **`TestNetworkConnectivityResult`** (3 теста):
   - `test_initialization` - инициализация объектов результатов
   - `test_add_check` - добавление результатов проверок
   - `test_get_summary` - генерация сводных отчетов
   - `test_get_summary_empty` - обработка пустых результатов

3. **`TestBaseInspectorClass`** (ожидается расширение):
   - Базовый класс для инспекторов (ядро бизнес-логики)

### Тестовая инфраструктура

#### 🛠️ **Инструменты:**
- **pytest** - основной фреймворк тестирования
- **pytest-cov** - измерение покрытия кода
- **pytest-asyncio** - поддержка асинхронных тестов
- **httpx** - HTTP клиент для API тестов
- **black** - автоформатирование кода
- **flake8** - линтинг и проверка стиля

#### 🏗️ **Docker среда:**
- Изолированная среда выполнения тестов
- Автоматическая установка зависимостей
- Генерация отчетов о покрытии
- Совместимость с CI/CD пайплайнами

#### 📊 **Метрики качества:**
- **Покрытие кода**: 21% (фокус на критических компонентах)
- **Количество тестов**: 13 тестов
- **Статус**: ✅ Все тесты проходят
- **Предупреждения**: 0 (чистый код)

### Фикстуры тестирования

```python
# conftest.py содержит готовые фикстуры:
@pytest.fixture
def temp_data_dir():          # Временная директория для данных
@pytest.fixture
def mock_env_vars():          # Переменные окружения
@pytest.fixture
def sample_cluster_data():    # Пример конфигурации кластера
@pytest.fixture
def mock_k8s_client():        # Mock Kubernetes API
@pytest.fixture
def mock_ssh_connection():    # Mock SSH соединения
@pytest.fixture
def test_app():               # FastAPI тестовое приложение
```

## Документация

- **[Backend README](backend/README.md)**: Подробная информация о серверной части приложения.
- **[Frontend README](frontend/README.md)**: Подробная информация о клиентской части приложения.

## Быстрый старт

### Метод 1: Docker Compose (Рекомендуется)

```bash
# Клонировать репозиторий
git clone https://github.com/optical4eye/kubeeye.git
cd kubeeye

# Запустить тесты (рекомендуется перед запуском)
./run_test.sh

# Запустить сервисы
docker-compose up -d

# Доступ к приложению по адресу http://localhost
```

### Метод 2: Kubernetes с Helm

```bash
# Добавить Helm репозиторий (если применимо)
# helm repo add kubeeye https://optical4eye.github.io/kubeeye

# Установить с Helm
helm install kubeeye ./chart/kubeeye

# Доступ через NodePort (по умолчанию: 30693)
# Или настроить Ingress для доступа по домену
```

## Переменные окружения

### Конфигурация Backend

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `PYTHONPATH` | `/app` | Путь поиска модулей Python |
| `KUBEEYE_DATA_DIR` | `/app/data` | Директория для хранения данных приложения (кластеры, отчеты и т.д.) |
| `KUBEEYE_REPORT_RETENTION_DAYS` | `7` | Количество дней хранения отчетов инспекции перед очисткой |

### Конфигурация SSH (унифицированная)

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `KUBEEYE_SSH_CONNECTION_TIMEOUT` | `10` | Таймаут SSH соединения в секундах |
| `KUBEEYE_SSH_MAX_CONCURRENT_CHECKS` | `20` | Максимальное количество одновременных SSH проверок |
| `KUBEEYE_SSH_COMMAND_TIMEOUT` | `30` | Таймаут выполнения SSH команды в секундах |
| `KUBEEYE_SSH_RETRY_ATTEMPTS` | `2` | Количество повторных попыток SSH соединения |
| `KUBEEYE_SSH_RETRY_DELAY` | `1` | Задержка между повторными попытками в секундах |
| `KUBEEYE_SSH_POOL_ENABLED` | `true` | Включить пул SSH соединений |
| `KUBEEYE_SSH_POOL_SIZE` | `10` | Размер пула SSH соединений |
| `KUBEEYE_SSH_POOL_CONNECTION_TIMEOUT` | `300` | Таймаут соединения в пуле SSH (секунды, 5 минут) |
| `KUBEEYE_SSH_POOL_KEEPALIVE_INTERVAL` | `60` | Интервал keepalive для пула SSH (секунды) |
| `KUBEEYE_SSH_KEEP_ALIVE` | `true` | Поддерживать SSH соединения активными |
| `KUBEEYE_SSH_VERBOSE` | `false` | Подробное логирование SSH |
| `KUBEEYE_SSH_LOG_OUTPUT` | `false` | Логировать вывод SSH команд |

### Конфигурация GitOps

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `KUBEEYE_GITOPS_REPO_NAME` | `kubeeye_rules` | Идентификатор имени для репозитория правил GitOps |
| `KUBEEYE_GITOPS_REPO_URL` | `https://github.com/optical4eye/kubeeye-rules.git` | URL репозитория правил GitOps |
| `KUBEEYE_GITOPS_REPO_BRANCH` | `main` | Ветка репозитория правил GitOps для использования |
| `KUBEEYE_GITOPS_REPO_USERNAME` | `optical4eye` | Имя пользователя для аутентификации в репозитории GitOps |
| `KUBEEYE_GITOPS_REPO_TOKEN` | `""` | Персональный токен доступа для репозитория GitOps |
| `KUBEEYE_GITOPS_REPO_DESCRIPTION` | `kubeeye repo rules` | Описание репозитория правил GitOps |
| `GIT_SSL_NO_VERIFY` | `false` | Отключить проверку SSL сертификатов для Git операций (использовать с осторожностью в продакшене) |

### Конфигурация Frontend

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `REACT_APP_API_URL` | `http://localhost:8000` | URL backend API для связи с frontend |

### Примеры конфигурации SSH

**Для медленных сетей или VPN соединений:**
```bash
export KUBEEYE_SSH_CONNECTION_TIMEOUT=30
export KUBEEYE_SSH_MAX_CONCURRENT_CHECKS=5
```

**Для больших кластеров (20+ узлов):**
```bash
export KUBEEYE_SSH_CONNECTION_TIMEOUT=15
export KUBEEYE_SSH_MAX_CONCURRENT_CHECKS=20
```

**Для разработки с расширенным хранением отчетов:**
```bash
export KUBEEYE_REPORT_RETENTION_DAYS=7
```